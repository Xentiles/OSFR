# OSFR Unity assets

Everything under this directory is owned by the OSFR project. Template and render-pipeline assets remain outside this directory until deliberately adopted or replaced.

The runtime assembly starts with analytical footprint calculations that can be tested without a camera or GPU. Render Graph integration will build on those verified equations.
